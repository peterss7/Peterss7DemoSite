peterss7-demo-site.com, www.peterss7-demo-site.com {
  encode gzip

  # API first
  handle_path /api/* {
    reverse_proxy api:8080
  }

  # Everything else = SPA
  handle {
    root * /srv
    try_files {path} /index.html
    file_server
  }
}
